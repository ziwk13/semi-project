<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.passwordResetTokenMapper">

  <!-- 토큰 발급: token 컬럼에는 원문이 아니라 SHA-256 해시를 저장한다 -->
  <insert id="insertToken" parameterType="PasswordResetTokenDTO">
    INSERT INTO password_reset_token (user_id, token, expires_at)
    VALUES (#{userId}, #{tokenHash}, #{expiresAt})
  </insert>

  <!-- 해시로 유효한(만료 전 + 미사용) 토큰 조회 -->
  <select id="findValidByHash" parameterType="String" resultType="PasswordResetTokenDTO">
    SELECT token_id, user_id, token AS token_hash, expires_at, used, created_at
      FROM password_reset_token
     WHERE token = #{tokenHash}
       AND used = 0
       AND expires_at > NOW()
  </select>

  <!-- 1회성 보장: 사용 처리 -->
  <update id="markUsed" parameterType="java.lang.Integer">
    UPDATE password_reset_token
       SET used = 1
     WHERE token_id = #{tokenId}
  </update>

</mapper>
